const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const seed=[{name:'Anna',email:'rodzina@blisko.pl',password:'demo123',role:'family'},{name:'Anna',facility:'Zielona Przystań',email:'placowka@blisko.pl',password:'demo123',role:'provider'}];let role='family',mode='login';function accounts(){try{return [...seed,...JSON.parse(localStorage.getItem('blisko_accounts')||'[]')]}catch{return seed}}function toast(t){$('#toast').textContent=t;$('#toast').classList.add('show');clearTimeout(window.at);window.at=setTimeout(()=>$('#toast').classList.remove('show'),2400)}function paint(){$$('.roleTabs button').forEach(b=>b.classList.toggle('active',b.dataset.role===role));$$('.modeTabs button').forEach(b=>b.classList.toggle('active',b.dataset.mode===mode));$$('.registerOnly').forEach(x=>x.hidden=mode!=='register');$$('.providerOnly').forEach(x=>x.hidden=role!=='provider');$('.submit').textContent=mode==='login'?'Zaloguj się →':'Utwórz konto →';$('[name=password]').autocomplete=mode==='login'?'current-password':'new-password'}$$('[data-role]').forEach(b=>b.onclick=()=>{role=b.dataset.role;paint()});$$('[data-mode]').forEach(b=>b.onclick=()=>{mode=b.dataset.mode;paint()});$$('[data-demo]').forEach(b=>b.onclick=()=>{role=b.dataset.demo;mode='login';paint();$('[name=email]').value=role==='family'?'rodzina@blisko.pl':'placowka@blisko.pl';$('[name=password]').value='demo123'});$('#forgot').onclick=()=>toast('W wersji demonstracyjnej reset hasła nie wysyła wiadomości. Użyj konta demo.');$('#authForm').onsubmit=e=>{e.preventDefault();let v=Object.fromEntries(new FormData(e.currentTarget));if(mode==='register'){if(accounts().some(a=>a.email.toLowerCase()===v.email.toLowerCase()))return toast('Konto z tym adresem już istnieje');let account={name:v.name||'Użytkownik',facility:v.facility||'',email:v.email.toLowerCase(),password:v.password,role};let saved=[];try{saved=JSON.parse(localStorage.getItem('blisko_accounts')||'[]')}catch{}saved.push(account);localStorage.setItem('blisko_accounts',JSON.stringify(saved));localStorage.setItem('blisko_session',JSON.stringify({...account,password:undefined}));toast('Konto demonstracyjne utworzone');setTimeout(()=>location.href=role==='provider'?'dashboard.html':'index.html',500)}else{let account=accounts().find(a=>a.email.toLowerCase()===v.email.toLowerCase()&&a.password===v.password&&a.role===role);if(!account)return toast('Nieprawidłowy e-mail, hasło lub typ konta');localStorage.setItem('blisko_session',JSON.stringify({...account,password:undefined}));toast('Zalogowano');setTimeout(()=>location.href=role==='provider'?'dashboard.html':'index.html',400)}};paint();

